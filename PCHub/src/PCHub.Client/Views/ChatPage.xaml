<UserControl x:Class="PCHub.Client.Views.ChatPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="💬 Chat Support" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                <TextBlock x:Name="TxtSubtitle" Text="Chat with Koh Dedi - your virtual assistant" 
                           FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="🔄 New Chat" Style="{StaticResource NeoButton}" FontSize="11" 
                        Padding="10,6" Margin="0,0,4,0" Click="NewChat_Click"/>
                <Button Content="🗑️ Clear" Style="{StaticResource NeoButton}" FontSize="11" 
                        Padding="10,6" Click="ClearChat_Click"/>
            </StackPanel>
        </Grid>

        <!-- Info Bar -->
        <Border Grid.Row="1" Background="{StaticResource TimerBgBrush}" CornerRadius="8" 
                Padding="10,8" Margin="0,0,0,8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🤖" FontSize="16" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Margin="8,0,0,0">
                    <TextBlock x:Name="TxtBotInfo" Text="Koh Dedi" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextBrush}"/>
                    <TextBlock Text="Online - ready to help!" FontSize="10" Foreground="{StaticResource SuccessBrush}"/>
                </StackPanel>
                <TextBlock Grid.Column="2" x:Name="TxtSessionId" Text="" FontSize="9" 
                           Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Chat Messages -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" x:Name="ChatScroll" 
                      Background="{StaticResource BgBrush}">
            <StackPanel x:Name="MessagesPanel" Margin="0,4"/>
        </ScrollViewer>

        <!-- Input Area -->
        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="MessageBox" Style="{StaticResource NeoTextBox}" 
                     VerticalAlignment="Center" KeyDown="MessageBox_KeyDown"
                     TextWrapping="Wrap" AcceptsReturn="True" MinHeight="40" MaxHeight="100"/>
            <Button Grid.Column="1" Content="📤 Send" Style="{StaticResource PrimaryButton}" 
                    Width="80" Height="40" Margin="8,0,0,0" Click="SendBtn_Click"/>
        </Grid>
    </Grid>
</UserControl>
