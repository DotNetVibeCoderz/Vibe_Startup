<UserControl x:Class="PCHub.Client.Views.ReservationsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <!-- Header -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="📅 My Bookings" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                    <TextBlock x:Name="TxtBookingCount" Text="Loading..." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                </StackPanel>
                <Button Grid.Column="1" Content="+ New Booking" Style="{StaticResource PrimaryButton}" 
                        Width="150" FontSize="13" Click="NewBooking_Click"/>
            </Grid>

            <!-- Bookings List -->
            <ItemsControl x:Name="ReservationsList"/>

            <!-- Empty State -->
            <Border x:Name="EmptyState" Style="{StaticResource NeoCard}" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="📅" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock Text="No bookings yet" FontSize="16" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="{StaticResource TextBrush}"/>
                    <TextBlock Text="Book a PC for your next gaming session!" 
                               FontSize="12" HorizontalAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" 
                               Margin="0,4,0,16"/>
                </StackPanel>
            </Border>

            <!-- Loading -->
            <TextBlock x:Name="TxtLoading" Text="Loading bookings..." FontSize="12" 
                       Foreground="{StaticResource TextSecondaryBrush}" 
                       HorizontalAlignment="Center" Margin="0,12,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
