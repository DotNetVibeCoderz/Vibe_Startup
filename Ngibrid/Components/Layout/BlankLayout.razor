@inherits LayoutComponentBase

@* Chrome-free layout for authentication pages — no sidebar, no nav.
   Theme comes from <html data-theme>, so this wrapper carries no theme class. *@
<div style="min-height:100vh;background:var(--bg-primary);">
    @Body
</div>
