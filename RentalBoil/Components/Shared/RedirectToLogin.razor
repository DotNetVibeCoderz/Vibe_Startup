@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"/login/{Uri.EscapeDataString(Navigation.Uri.Replace(Navigation.BaseUri, "/"))}", true);
    }
}
