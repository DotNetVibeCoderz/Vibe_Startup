{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "_comment": "Operational configuration (API keys, connection strings, storage, AI providers) lives in config/cyberlens.settings.json and is fully editable from the in-app Settings page."
}
